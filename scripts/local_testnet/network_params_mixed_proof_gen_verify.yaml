# 3 nodes generate proofs, 1 node only verifies
participants:
  # Proof generating nodes (nodes 1-3)
  - el_type: geth
    el_image: ethereum/client-go:latest
    cl_type: lighthouse
    cl_image: lighthouse:local
    cl_extra_params:
      - --activate-zkvm
      - --zkvm-generation-proof-types=0,1
      - --target-peers=3
    count: 3
  # Proof verifying only node (node 4)
  # TODO(zkproofs): Currently there is no way to add no client here
  # We likely want to use our dummy zkvm EL here
  - el_type: geth
    el_image: ethereum/client-go:latest
    cl_type: lighthouse
    cl_image: lighthouse:local
    cl_extra_params:
      - --activate-zkvm
      - --target-peers=3
    count: 1
network_params:
  electra_fork_epoch: 0
  fulu_fork_epoch: 1
  seconds_per_slot: 2
global_log_level: debug
snooper_enabled: false
additional_services:
  - dora
  - prometheus_grafana

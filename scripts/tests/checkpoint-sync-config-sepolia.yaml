# Kurtosis config file to checkpoint sync to a live network (Sepolia).
participants:
  - cl_type: lighthouse
    cl_image: lighthouse:local
    el_type: geth
    el_image: ethereum/client-go:latest
    cl_extra_params:: ["--disable-backfill-rate-limiting"]
    supernode: true
  - cl_type: lighthouse
    cl_image: lighthouse:local
    el_type: geth
    el_image: ethereum/client-go:latest 
    cl_extra_para_ms:: ["--disable-backfill-rate-limiting"]
    supernode: false

checkpoint_sync_enabled: true
checkpoint_sync_url: "https://checkpoint-sync.sepolia.ethpandaops.io"

global_log_level: debug

network_params:
  network: sepolia

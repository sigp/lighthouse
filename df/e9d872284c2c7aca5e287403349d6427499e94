review response (content only, base unchanged), five items: (1) per-builder 200ms deadline on request-auth signing (a stalled signer is dropped via the existing log-and-omit path; value priced against the serial pre-publish chain); (2) envelope fetch/sign/publish gated on builder_index == BUILDER_INDEX_SELF_BUILD (a builder-won bid's envelope is revealed by the builder; resolves the stale local-build TODO); (3) builder-preference submissions grouped and labeled by the fork of their entries' epoch (fork-boundary lookahead no longer goes out under the pre-fork version); (4) elapsed duties skipped before signing (no per-slot re-sign/re-submit of dead entries for the rest of the epoch); (5) stale produceBlockV4 TODO in build_block_contents resolved with a current-truth comment. Tests for 1-4; rig gains builder-preference mocks with SSZ body capture. See: git range-diff fb8c9a469^..fb8c9a469 dfe9d8722^..dfe9d8722

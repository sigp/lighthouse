review response (content only, base unchanged): builder-preferences endpoint test coverage (missing Eth-Consensus-Version header, zero-length url/auth-data entries, oversize list, no-builder-service), all run release-gated under FORK_NAME=gloas; a node without a builder service now rejects preferences with a descriptive 400 instead of a 500; SSZ Content-Type detection tolerates media-type parameters and whitespace (shared is_ssz_content_type helper for both POST bodies). See: git range-diff a03f18662^..a03f18662 81335acd8^..81335acd8
